import { LoaderBackground, LoaderComponent } from "./styles";

const Loading = () => {
  return (
    <LoaderBackground>
      <LoaderComponent />;
    </LoaderBackground>
  );
};

export default Loading;
