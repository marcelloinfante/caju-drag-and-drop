import { createContext } from "react";

import { RegistrationContextType } from "./types";

const RegistrationContext = createContext({} as RegistrationContextType);

export default RegistrationContext;
